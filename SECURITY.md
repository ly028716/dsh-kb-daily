# 安全策略

## 支持版本

| 版本 | 安全修复状态 |
| --- | --- |
| `0.1.0-rc.8` | 当前维护线，优先修复高风险问题 |
| `0.2.x` | 计划中的下一条维护线，发布后更新此表 |
| 其他版本 | 不保证提供安全修复 |

## 报告漏洞

请不要创建公开 Issue，也不要在讨论区、Pull Request、日志或截图中公开漏洞细节。当前仓库未配置专用安全邮箱时，请通过 GitHub Security Advisories 的私下报告入口联系维护者；如果该入口不可用，请先提交不包含漏洞细节的维护者联系请求。

报告中只提供维护者复现所需的最小信息：

- 受影响的版本、操作系统和 DSH 版本；
- 不含真实数据的最小复现；
- 影响范围和安全后果；
- 是否已经公开或正在被利用。

请删除 API key、访问令牌、真实 vault 路径、笔记内容和个人信息。不要为了复现而上传真实知识库或完整日志。

## 处理流程

维护者会先确认收到报告，复现并评估影响，必要时准备修复和回归测试，再协调披露时间。报告者可以在修复发布后请求署名或保持匿名。处理时间取决于复现质量、影响范围和是否需要上游 DSH 修复。

## 安全边界

本插件默认只访问配置的 vault，并通过路径 containment、symlink/junction 检查、独占报告写入和宿主审批限制写入边界。不要把它配置到整个磁盘、用户目录或包含不应发送给模型的内容的路径。
